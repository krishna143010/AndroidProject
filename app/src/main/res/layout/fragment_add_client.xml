<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    tools:context=".AddClientFragment">

    <TextView
        android:id="@+id/heading"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:paddingTop="25dp"
        android:text="Add a Client"
        android:textColor="@color/purple_500"
        android:textAlignment="center"
        android:textSize="24dp" />

    <EditText
        android:id="@+id/clientName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/heading"
        android:ems="10"
        android:hint="Enter Client Name"
        android:inputType="textPersonName"
        android:paddingTop="50dp"
        android:text=""
        android:textAlignment="center" />

    <Button
        android:id="@+id/addClientButtonView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/clientName"
        android:textAlignment="center"
        android:layout_marginLeft="135dp"
        android:text="Add Client"
        android:layout_marginTop="100dp"
        android:textSize="14sp" />

    <TextView
        android:id="@+id/spinnerError"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:paddingStart="16dp"
        android:layout_below="@id/addClientButtonView"
        android:text="Client Names as External is Reserved"
        android:textSize="14dp"
        android:textColor="?android:attr/colorMultiSelectHighlight"></TextView>

</RelativeLayout>